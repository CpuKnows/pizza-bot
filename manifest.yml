---
declared-services:
  my-conversation-service:
    label: conversation
    plan: free
applications:
- name: pizza-bot
  command: npm start
  path: .
  memory: 256M
  instances: 1
  services:
  - pizza-bot-service
  env:
    NPM_CONFIG_PRODUCTION: false